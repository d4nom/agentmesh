# The deterministic mock provider is the default and requires no credentials.
LLM_PROVIDER=mock

# To use the live DeepSeek provider, set:
# LLM_PROVIDER=deepseek
# DEEPSEEK_API_KEY=replace-me

# Optional: select another incident-triage topology for the default agent services.
# SYSTEM_CONFIG_FILE=triage_with_summary.yaml
